name: Publish to Maven Central

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    env:
      ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.MAVEN_CENTRAL_USERNAME }}
      ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.MAVEN_CENTRAL_PASSWORD }}
      ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNING_KEY_PASSWORD }}
      ORG_GRADLE_PROJECT_signingInMemoryKeyId: ${{ secrets.SIGNING_KEY_ID }}
      ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.SIGNING_KEY }}

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-java@v5
        with:
          distribution: zulu
          java-version: 21

      - uses: gradle/actions/setup-gradle@v6

      # publishToMavenCentral, not `publish` or publishAllPublicationsToMavenCentral-
      # Repository: only this task drives the Portal deployment lifecycle, and therefore
      # only it honours the validateDeployment setting. The others upload a bundle and
      # exit, which is why rejected deployments used to leave a green run.
      - name: Publish treant-annotations and treant-compiler
        run: ./gradlew :treant-annotations:publishToMavenCentral :treant-compiler:publishToMavenCentral

      - name: Publish treant-gradle-plugin
        working-directory: treant-gradle-plugin
        run: ../gradlew publishToMavenCentral
